# HTTP port for Gin server inside container/host
PORT=8080

# SQLite database file path
# Local dev default:
# DB_PATH=/tmp/construction_ai.db
# Docker/Cloud default (with mounted volume at /data):
DB_PATH=/data/construction_ai.db
