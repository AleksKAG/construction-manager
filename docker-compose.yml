services:
  app:
    build:
      context: .
      dockerfile: deployments/Dockerfile
    ports:
      - "8080:8080"
    environment:
      ENV: production
      PORT: 8080
    restart: unless-stopped
